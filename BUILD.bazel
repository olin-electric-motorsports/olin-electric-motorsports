load("@com_github_bazelbuild_buildtools//buildifier:def.bzl", "buildifier")

exports_files([
    ".avrduderc",
])

buildifier(
    name = "buildifier",
    tags = ["manual"],
)

buildifier(
    name = "buildifier_check",
    mode = "check",
    tags = ["manual"],
)
